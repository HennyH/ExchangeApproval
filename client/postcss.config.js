module.exports = {
    plugins: {
        // process @import statements
        'postcss-import': {},
        // convert modern CSS into browser compatible CSS
        'postcss-preset-env': {},
        // minify
        'cssnano': {}
    }
}